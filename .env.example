# Optional: canonical URL for sitemap, robots, and Astro redirects (use your real domain on Vercel)
# PUBLIC_SITE_URL=https://www.example.com
#
# On Vercel you can omit this if "Automatically expose System Environment Variables" is ON
# (Settings → Environment Variables). Otherwise set PUBLIC_SITE_URL to your .vercel.app or custom domain.
