# Web3Forms — create a key at https://web3forms.com (free)
PUBLIC_WEB3FORMS_ACCESS_KEY=REPLACE_ME

# Optional: canonical / sitemap base URL (must match `site` in astro.config.mjs)
# PUBLIC_SITE_URL=https://your-domain.pages.dev
